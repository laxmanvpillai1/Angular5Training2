import { TrimTextPipePipe } from './trim-text-pipe.pipe';

describe('TrimTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
